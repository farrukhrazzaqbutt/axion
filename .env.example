# Copy to .env and fill in real values. NEVER commit .env to git.
# On Render: set these in Dashboard → Your Service → Environment.

# Service
SERVICE_NAME=axion
ENV=development
USER_PORT=5111

# MongoDB (database is auto-created on first use)
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@HOST.mongodb.net/axion?retryWrites=true&w=majority

# Redis (e.g. Upstash - use the Redis URL from dashboard)
CACHE_REDIS=rediss://default:PASSWORD@HOST.upstash.io:6379
CORTEX_REDIS=rediss://default:PASSWORD@HOST.upstash.io:6379
OYSTER_REDIS=rediss://default:PASSWORD@HOST.upstash.io:6379

# JWT – use long random strings in production (e.g. 32+ chars each)
LONG_TOKEN_SECRET=your-long-token-secret
SHORT_TOKEN_SECRET=your-short-token-secret
NACL_SECRET=your-nacl-secret
